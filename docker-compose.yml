services:
  bot:
    build: .
    container_name: avito-bot
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/data/data.db
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
